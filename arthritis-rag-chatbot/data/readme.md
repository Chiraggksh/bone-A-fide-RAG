# Data

Raw research PDFs are not included in this repository to keep the project lightweight.

The knowledge base has already been processed into embeddings and stored inside the `vector_store/` directory for direct use.

This enables quick setup and immediate testing of the RAG medical chatbot without running the ingestion pipeline again.
